## Visual Studio / local junk only
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates
TestResults/
*.VisualState.xml
*.Cache
*.log

# Keep build outputs and game deps in repo (per project policy)
# bin/, obj/, 依赖/ are tracked
